# 装备槽修正简报 2026-07-26z（戒指图标一次性完整修复）

戳记：`RING_ICON_ONESHOT_20260726z`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
SHA256：`06DE577D4AFB879514080149AEF33BCFBBE5D8497A7D53506F8EAD7D0AE6C3CD`（out ≡ 客户端）  
配置：`pendant2_ui=true`

## 1. 为何 y 仍无图标（根因）

| 核查 | 结果 |
|------|------|
| Client_1 DLL 是否 y | **是** — SHA ≡ `out\Release`，串 `FIX_RING_DRAW_20260726y` |
| 经典戒 GetSlotXY 硬编码 | y 已写 BP12/13/15/16 ← HitTest 标签坐标 |
| 绘制循环上界 | **仍为** `setne` + `add r,0x32` @ `7FEFB9`/`7FEE54` → max = **50+flag ≤ 51** |
| BP52/53 | HitTest/坐标已写，但 **循环永不访问** → −52/−53 已装备也无图标 |
| 经典 4 戒空 | 若 26x/`InventoryManipulator` 把戒路由到 −52/−53，标签格无物 → 也像「全无图标」 |

**结论**：y 已加载且经典 XY 修对了；缺的是 **安全把角色装备栏绘制上界扩到 BP53**，并恢复 `get_bodypart×6` 与三表坐标对齐。不是「y 没部署」。

## 2. 本次完整修复（一次性）

### 客户端

| 项 | 做法 |
|----|------|
| 绘制循环 | **`add imm 0x32→0x34`** @ `7FEFBC` + `7FEE54`（ExpectBytes；**setne 不动**）→ flag=1 时 max=**53** |
| 对比禁区 | **不是** ForceDrawLoop（`setne`→`mov al,1` @ `7FEFB9`）；**不是** pet HT `801214`/`8013A3` |
| 经典戒 | GetSlotXY + HitTest + BE27E0：15/16=(104/137,68)，12/13=(104/137,167) |
| 红 3/4 | GetSlotXY + HitTest + BE27E0：52/53=(104/137,35) |
| HitTest 步行 | 仍 `7FEC6F` 末址 → `BE240C`（BP53） |
| `get_bodypart` 111 | **ON** `{12,13,15,16,52,53}`（writeAll 大缓冲，26x PE 已证） |
| `is_correct` / AbleToWear 111 | 保持放行 / call-orig-then-OK |
| 武器/盾/耳 / 护肩⑧ / 坠⑦ | 未改 26w 逻辑 |

### 服务端

| 项 | 状态 |
|----|------|
| `EquipSlot.RING` −52/−53 | 保持 |
| `addInventoryInfo` 放行 | 保持 |
| 双击满 4 戒路由 −52/−53 | 保持 |
| 注释戳记 | 更新为 z |

## 3. PE 字节核对（Client_1 BeiDou.exe）

```
7FEFB9: 0F 95 C0          setne al          ← 禁止改成 B0 01
7FEFBC: 83 C0 32          add eax, 0x32     ← 改为 83 C0 34
7FEE54: 83 C1 32          add ecx, 0x32     ← 改为 83 C1 34
7FEC6F: 81 F9 F4 23 BE 00 cmp ecx, BE23F4   ← 已/仍改为 BE240C
8013A3: cmp edx, 0x32                       ← 宠物，禁止动
```

`GetSlotXY` @ `7FEFEA`：flag≠0 → `BE2580[index]`（index=BP−1）。故 index 51/52 必须先写好坐标再扫绘制。

## 4. 明确没做的事

1. 未 ForceDrawLoop / 未 DllMain 挂 UI  
2. 未改 pet HitTest  
3. 未动错 VA / 未改 setne  
4. 未扩 `g_aEquipSlotPos` 表体重分配  
5. 未用 expanded (336,165) 布局  

## 5. 验证清单

1. SHA：`out\Release\ijl15.dll` ≡ Client_1；串 **`RING_ICON_ONESHOT_20260726z`**（无 y）  
2. `pendant2_ui=true`；Login→选角→进图：**无** ALL_IDLE / E_POINTER  
3. **戒指×2** y=68、**指环×2** y=167：已装备图标在对应标签上  
4. 拖戒到红 3/4 → −52/−53，图标在 (104,35)/(137,35)，无红  
5. 双击自动穿：空位填满 4 经典后第 5/6 进 −52/−53 且有图标  
6. 武器「武器」、盾「盾牌」；护肩红 8；第二坠红 7  
7. 换图/重登 −52/−53 仍在栏且可见  

## 6. 回退

若进图 E_POINTER：把 `kEnableExtraRingsBp52_53` 改回 `false`，**保留** `PatchCharDrawLoopForBp53`（仅显示已装备的 52/53）；再查具体 writeAll 调用点。  
若登录 ALL_IDLE：确认未误改 `7FEFB9` 的 `setne`；回退 dll 到 y 并报告。
