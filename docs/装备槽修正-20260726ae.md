# 装备槽修正简报 2026-07-26ae（FIX_AD_HANG — 恢复响应 + 保留红 3/4 修复）

戳记：`FIX_AD_HANG_20260726ae`
SHA256：`BDF8A15D8BD7844856896BDA7B9173FF2FE7A4B894404A5AB022E77A916DA99B`（out ≡ Client_1）  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
配置：`pendant2_ui=true`  
紧急回退：`_ijl15_backup\ijl15.dll.20260727_ad_from_ac.bak`（ac）

## 1. ad 卡死根因（PE 已证）

| 现象 | 根因 |
|------|------|
| 客户端打开 / E 开装备栏 **未响应** | draw add-imm **`0x35` + flag1** ⇒ max BP=**54**；BP54 走 OOB walker / special `cmp [ecx+edx]` → UI 线程故障 |
| ForceNormal「装了」却无效 | VA 写成 **`7FEEC5`**（`jz` 的 rel8 立即数），真起点是 **`7FEEC2`**（`85 F6 74 15 8B 4D DC`）→ ExpectBytes 失败 → cave **从未安装** |
| 非 DllMain 早挂 / 非 aa 的 `[ebp-14]` 十进制 | Client_1 当时确为 `RING_34_ONESHOT_20260726ad` SHA `18100E01…` |

## 2. ae 修复

| 项 | 做法 |
|----|------|
| 绘制上界 | loop-end `@7FEFB9`：`setne+add` → **`mov eax,53; nop`**（必含 BP53、永不 54；≠ ForceDrawLoop 的 `mov al,1`） |
| early add | 仍 `0x35`（仅入口门闸；循环体由固定 53 截断） |
| ForceNormal | **`@7FEEC2`**，仅 BP **52\|53** |
| DrawHasItem | 保持 edi=item* + `[ebp-14h]` + 清红；**BP>53 空槽不解引用**；walk 前 `test ecx,ecx` |
| 库存 bind | 同 ac/ad：bound→−53；−52 空隙；−53→`g_ring53ZRef` |

## 3. 验证清单

1. Client_1 串 **`FIX_AD_HANG_20260726ae`**（无 ad）
2. 进图后 **E 开装备栏不卡死**；客户端启动不未响应
3. 红 3/4：有图标、无红（或明显改善）、tip、不可无限吞
4. 经典 4 戒 / 武器盾 / 护肩⑧ / 第二坠⑦ 正常

## 4. 回退

- 若 ae 仍卡：立刻拷回 `ijl15.dll.20260727_ad_from_ac.bak`（ac，3/4 部分退化可接受）
