# FIX_DBL_UNEQUIP_COMBAT_SHADOW_20260729c

部署：`out\Release\ijl15.dll` → BeiDou-Client_1（SHA 一致）  
戳记：`FIX_DBL_UNEQUIP_COMBAT_SHADOW_20260729c`  
服务端：无需改动（`recalcEquipStats` 已遍历 EQUIPPED 含 −52/−53；拖拽卸装已通）

## 根因

| 现象 | 根因 |
|------|------|
| 双击卸装仍失败（拖拽 OK） | 仅靠 `wear()` + ItemTI/occ B 不够：装备栏双击在空搜/占用链上仍易失败；拖拽走 `SendChangeSlotPosition`（ITEM_MOVE src&lt;0 dest&gt;0） |
| 红 3/4 属性不进面板 | combat/localstat `@77F879` 即使用 bound→−55，仍用原生 `Char` ZRef 算址；−52/−53 空隙空 → `cmp [edi+4],0` 跳过加算。UI/GetItem 走影子故「看得见无属性」 |

## 修复（客户端 only）

| 改动 | 说明 |
|------|------|
| `OnDoubleClicked_EquippedUnequip_hook` | **主路径** `SendChangeSlotPosition@A0900A(TI=1, src=slot, dest=首个空正槽, qty=1)`；失败再 `wear`；禁止回落 6-BP 空搜 |
| `Combat_OccShadow_cave@77F879` | −52..−55 → `g_ring52/53` / badge / totem；join `@77F894` |
| 保留 | wear occ A/B/Cash、EmptySearch cave、`Shoulder_Rehook` 最外层 |

## 验证清单

1. 戳记 `FIX_DBL_UNEQUIP_COMBAT_SHADOW_20260729c`，`out\Release` SHA ≡ Client_1
2. **完全退出**客户端再开（进图后 Rehook 才挂上）
3. 装备栏双击：帽子、经典戒、红 3/4 → 卸回装备背包（需有空位）
4. 穿上带 STR/DEX 的红 3/4 戒 → 面板属性增加；卸下减少
5. 拖拽卸装 / 背包双击穿戒 / 重登红 3/4 仍在：无回归
