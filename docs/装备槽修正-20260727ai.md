# 装备槽修正简报 2026-07-27ai（背包双击戒优先空 3/4）

戳记：`FIX_RING_EMPTY34_20260727ai`  
SHA256：`B696C72F7D02FF119936CA9163B706F059A7C456B20DE137AECA44AED5425930`（out ≡ 客户端）  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
配置：`pendant2_ui=true`  
备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260727_ai_from_ah.bak`  
服务端：**需重建**（`InventoryManipulator.equip` 六戒空槽优先路由）

## 1. 根因

| 侧 | 根因 |
|----|------|
| **Client（主因）** | `OnDoubleClicked` 在 `get_bodypart(writeAll)` 写出 6 BP 后，宠物门控（`BP0≤20`）跳到 `push 1; pop edi`，**把 empty-search 计数砸成 1** → 只 `wear(bag, BP12)` = 替换 −12，从不扫 BP52/53 |
| **Server（兜底缺口）** | 旧逻辑仅当 `dst==−12 && 经典4满` 才改到 −52/−53；其它替换 dst / 经典未满时仍会卸经典戒 |

## 2. 修复

| 位置 | 行为 |
|------|------|
| `get_bodypart` 111 | 顺序改为 `{52,53,12,13,15,16}`（空 3/4 优先） |
| `DblClick_EquippedUnequip_cave` inventory | 若 `anBodyPart` 为戒列表且 `[1]≠0` → `edi=6` 再走原生找空；装备栏双击卸装（ah）不变 |
| `InventoryManipulator.equip` | 戒且 `dst` 已占用 → 按 `−52/−53` 再经典 4 找空槽；6 满才替换 |

保留 ah/ag：双击卸装、OccBp53、bind、DrawHasItem、ForceNormal+edx、max=53。

## 3. 验证清单

1. 戳记 `FIX_RING_EMPTY34_20260727ai`，DLL SHA = `out\Release`，`pendant2_ui=true`；服务端已热更/重启
2. 经典 4 戒已穿、红 3/4 空 → **背包双击戒** → 进 −52（再 −53），**不卸**经典
3. 仅 −52 空、−53 与经典满 → 双击进 −52
4. 6 槽全满 → 双击才替换（可接受）
5. 装备栏双击戒/红3/4 仍可卸回背包；E 无卡；图标/tip 正常
6. 武器/盾/护肩红8 / 第二坠：无回归

