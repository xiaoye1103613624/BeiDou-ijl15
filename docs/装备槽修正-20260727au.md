# FIX_RING_DBLCLICK_ROTATE_20260727au

部署：`out\Release\ijl15.dll` → BeiDou-Client_1  
配置：`pendant2_ui=true`  
服务端：`InventoryManipulator.equip` 六戒全满轮询兜底（需热更/重启）

## 需求

六戒背包双击：**先找空位**（红 3/4 优先于经典 4，ai/at 已有）；**六槽全满**时不再静默失败，而是**按顺序轮询替换**。

## 修复

| 侧 | 改动 |
|----|------|
| Client `@4F0B85` | 空槽搜索耗尽时 `DblClick_RingFullRotate_cave`：戒列表则 `ecx = rotate % 6`，jmp wear `@4F0B89` |
| Client | 轮询顺序与 `get_bodypart` 一致：`{52,53,12,13,15,16}` |
| Server | `preferEmpty` 后仍占用且非点装戒 → `pickSixRingRotateSlot` 按 `{-52,-53,-12,-13,-15,-16}` 轮询 |

保留：at OccBp 保 eax/ecx、as 影子、空槽优先、双击卸装。

## 验证

1. 戳记 `FIX_RING_DBLCLICK_ROTATE_20260727au`，`out\Release` SHA ≡ Client_1
2. 经典 4 满、红 3/4 空 → 双击进红 3/4（不顶经典）
3. 六槽全满 → 连续双击背包戒，依次替换红3→红4→−12→−13→−15→−16→红3…
4. 装备栏双击卸装、拖拽、重登 3/4 仍在、E 无卡
