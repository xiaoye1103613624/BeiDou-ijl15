# 装备槽修正简报 2026-07-27al（穿卸 + 3/4 重登）

戳记：`FIX_WEAR_PERSIST_20260727al`  
SHA256：`7E095A534387C1A8843DF941609D7A28F366B8EF5820181A345D12164CD3102B`（`out\Release` ≡ Client_1）  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`  
配置：`pendant2_ui=true`  
备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260727_090442.bak`  
服务端：**无需重建**（DB / `addInventoryInfo` / `EquipSlot.RING` 已支持 −52/−53；护肩 −20 同链路）

## 1. 根因

| 现象 | 根因 |
|------|------|
| **穿卸又坏**（ak 后） | `ak` 先把 Get/Set/combat bound 提到 **−55**，再 `ExpectBytes` 装地址 cave。若 cave 未装上，−53/−54/−55 走原生 ZRef 计算 → **砸 TSecType**（Char+0x293）→ 穿卸/库存状态损坏。 |
| **重登/重启后红 3/4「没了」** | **不是没落库**。SQL 已有 `position=-52/-53`（与护肩 −20 一样）。客户端 apply 需 max≥53 + SetItem 影子；bound/cave 顺序错会导致 −53 影子未写入，看起来像「没持久化」。 |
| 对比护肩 −20 | 服务端 ItemFactory / `addInventoryInfo` 已放行；−20 原生槽无影子依赖，故一直正常。 |

## 2. 修复（客户端）

| 项 | 行为 |
|----|------|
| Get/Set 地址 cave | **先**安装，校验首字节为 `E9`，**再**把 bound 提到 −55；失败则只提到 −52（不解锁影子槽） |
| 登录 apply | `@4E5D0C`/`@4E5DCC`：`cmp esi` imm 51..55 → **55** |
| 绘制上界 | shoulders **DllMain** 即 `mov eax,55;nop` @`7FEFB9`（不单靠 pendant2 进图后） |
| 双击 | 保留 aj：`slot<0` → jmp `@4F0B89`；背包戒 `edi=6` 空槽优先 |

保留：徽章/图腾 BP54/55 影子、Occ、ForceNormal+edx、walk 仅 −52。

## 3. 验证清单

1. 戳记 `FIX_WEAR_PERSIST_20260727al`，SHA ≡ Client_1，`pendant2_ui=true`
2. 背包双击戒 → 空 3/4 优先；装备栏双击卸回背包
3. 穿 −52/−53 → 正常退出/重登：红 3/4 仍在；护肩 −20 无回归
4. DB：`SELECT position,itemid FROM inventoryitems WHERE inventorytype=-1 AND position IN (-20,-52,-53,-54,-55);`
5. E / 第二坠 / 红 9/10：无卡死

## 4. 证据（本次）

```
characterid=1: (-53,1113068), (-52,1113069), (-20,1152199)  — DB 非空
```
