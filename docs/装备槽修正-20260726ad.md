# 装备槽修正简报 2026-07-26ad（红 3/4 一次性完整修复）

戳记：`RING_34_ONESHOT_20260726ad`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
SHA256：`18100E01B792E509672DDF8E9B1CF1CA9F064B0C2F38D8EE5C38D9FA91220690`（out ≡ 客户端）  
配置：`pendant2_ui=true`  
备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260727_ad_from_ac.bak`

## 1. 相对 ac 仍坏的根因（有证据）

| 现象 | 根因 |
|------|------|
| 红 4 无图标 / 可连续拖入 | draw imm `0x34` ⇒ max=52+Character+0x5E8；**CUIEquip 旗 ≠ Character+0x5E8**，常为 0 → **BP53 永不进绘制循环** |
| 红 4 即使进循环仍空 | DrawHasItem cave 只修了 ZF（影子 has_item），continue `@7FEEE2` 是 `mov esi,edi` —— **edi 仍是 TSecType 垃圾** → 不画真图标 / 槽位看起来空 |
| 红 3 有图标但红 | BP52/53 在 `test esi,esi` 时 esi 常为 TSecType walker 脏值 → 走 7FEEC9 特殊路径；强制进 cave 后清 `[ebp-18h]` 红标 |
| E 卡死（aa） | `[ebp-14]` 十进制 ≠ PE `[ebp-14h]`（`8B 4D EC`）—— **ad 仍只用 14h** |

## 2. 本次修复

### 客户端

| 项 | 做法 |
|----|------|
| 绘制上界 | imm `0x32→0x35` @ `7FEFBC`+`7FEE54`（flag0 必含 BP53；setne **不动**） |
| ForceNormal cave | `@7FEEC5`：BP≥52 时 `xor esi,esi` 强制进 `7FEEDB` |
| DrawHasItem cave | BP52/53：**edi=item*** + `[ebp-14h]` + 清红 `[ebp-18h]`；walk 仍 `[ebp-14h]` |
| −52/−53 库存 | 保持 ac：bound→−53；−52 原生空隙；−53→`g_ring53ZRef` |
| 禁止 | ForceDrawLoop / pet HT / expand 布局 / 部署 ezorsia\out |

### 服务端

无需改：`EquipSlot.RING` 已含 −52/−53；`addInventoryInfo` 已放行；满 4 戒路由保留。

## 3. 验证清单

1. SHA：`out\Release` ≡ Client_1；串 **`RING_34_ONESHOT_20260726ad`**（无 ac/aa）
2. `pendant2_ui=true`；进图后 **E 开装备栏不卡死**
3. 红 3（−52）：有图标、**无红**、悬停 tip、再拖换装/拒绝
4. 红 4（−53）：有图标、tip、**不能无限吞**；卸下正常
5. 经典 4 戒、武器、盾、护肩⑧、第二坠⑦ 正常

## 4. 回退

- 备份：`_ijl15_backup\ijl15.dll.20260727_ad_from_ac.bak`
- 若 E 再卡：确认 cave 无 `[ebp-14]` 十进制；回退该 bak
