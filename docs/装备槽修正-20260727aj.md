# 装备槽修正简报 2026-07-27aj（双击卸装 + 重登 3/4）

戳记：`FIX_RING_UNEQUIP_PERSIST_20260727aj`  
SHA256：`F93FE92385CAEFF24F85E0924C8F1D287ABB80B06B8F0D7ACC4280E84CCCDA64`（`out\Release` ≡ Client_1）  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`  
配置：`pendant2_ui=true`  
备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260727_025339.bak`  
服务端：**无需重建**（`addInventoryInfo` / `ItemFactory` / `EquipSlot.RING` 已支持 −52/−53；DB 已有落库证据）

## 1. 根因

| 现象 | 根因 |
|------|------|
| **双击卸装回归**（ai 后） | ah 卸装 cave：`call wear@4F1C2D` 后 join **`4F0B98`**，但原生 `call` 返回点是 **`4F0B9B`**（`test eax,eax`）；`4F0B98` 落在 `call` 的相对偏移中间 → 卸装路径损坏。装备栏多 BP 空槽搜索在 6 槽全满时也不会发卸装包。 |
| **重启后红 3/4「没了」** | **不是**「没落库 / 仅测试 GM」为主因。DB `inventoryitems` 对 char=1 已有 `position=-52/-53`。客户端登录应用路径存在 `cmp esi,51 / jg skip` 与装备遍历 `cmp ebx,-51`，会跳过/不扫扩展槽；−53 仍靠 `g_ring53ZRef`（不可把 raw walk 扩到 −53）。 |

## 2. 修复（客户端 only）

| 补丁 | 行为 |
|------|------|
| `DblClick_EquippedUnequip_cave` | `slot < 0`：`anBodyPart[0]=bp`，`ecx=0`，**jmp `4F0B89`**（原生单 BP wear/卸装）。`slot ≥ 0`：戒列表仍 `edi=6` 后走 `4F0B62` 空槽搜索（保 ai 空 3/4 优先）。 |
| `@4E5D0C` / `@4E5DCC` | `cmp esi,51` → `53`（登录/应用放行 BP52/53） |
| `@4E5AFA` | `cmp ebx,-51` → `-52`（raw ZRef 遍历含 −52；**不**扩到 −53） |

保留：OccBp53、bind、DrawHasItem、ForceNormal+edx、max=53、get_bodypart `{52,53,12,…}`、服务端空槽优先。

## 3. 验证清单

1. 戳记 `FIX_RING_UNEQUIP_PERSIST_20260727aj`，`out\Release` SHA ≡ Client_1，`pendant2_ui=true`
2. 装备栏双击经典戒 / 红 3 / 红 4 → 卸回装备背包（需空位）
3. 背包双击戒：红 3/4 空时仍优先进 −52/−53，不顶经典
4. 穿上 −52/−53 → **正常退出/切频道** → 重登：红 3/4 图标仍在
5. 可选 DB：`SELECT position,itemid FROM inventoryitems WHERE inventorytype=-1 AND position IN (-52,-53);`
6. E / tip / 护肩红 8 / 第二坠：无回归
