# 装备槽修正简报 2026-07-26x（六戒安全完成）

戳记：`SIX_RING_SAFE_20260726x`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
配置：`pendant2_ui=true`

## 1. 本次做了什么

### 客户端（ijl15 / shoulders + pendant2 UI）

| 项 | 做法 |
|----|------|
| `get_bodypart` 111 | 写 `{12,13,15,16,52,53}`（`kEnableExtraRingsBp52_53=true`） |
| `is_correct` 111 | 继续放行 12/13/15/16/52/53 |
| `IsAbleToWear` 111 | **shoulder 同款**：先 call-orig 再强制 OK（必走 cleanup，禁 early-return） |
| UI 红 3/4 | 仍为 `(104,35)` / `(137,35)` → BP52/53（pendant2 进图） |
| HitTest | 仍延伸至 `BE240C`（BP53）；**未动** pet HT `801214`/`8013A3` |
| 武器/盾/护肩/第二坠 | 未改 26w 逻辑：`GetSlotXY←HitTest`；护肩红 8；坠红 7 |

### 服务端

| 项 | 做法 |
|----|------|
| `EquipSlot.RING` | 已含 −52/−53 |
| `addInventoryInfo` | **放行** −52/−53/−152/−153（进图装备栏同步） |
| `addCharEquips` | 选角外观仍跳过扩展戒/第二坠（防选角解析） |
| `InventoryManipulator` | 原 4 戒满且打 −12/−112 时，空则改路由 −52/−53（双击兜底） |
| `recalcEquipStats` | 遍历 EQUIPPED，−52/−53 自然计入 |

## 2. 如何验证（PE / 部署）

1. **PE（Client_1 BeiDou.exe，非 v95 IDB）**  
   - `get_bodypart` 调用点 12 处；`writeAll=1` 帧为 `ebp-0xE8` / `-0x124` / `-0xFC`（大缓冲）。  
   - 仅 `ebp-0x10` 的调用点传 `writeAll=0`（只写 1 个 int）。  
   - 原生 111 在 writeAll 时已写 4×int；扩到 6 未做 `g_aEquipSlotPos` 重分配。
2. **部署**  
   - 构建 `Release|x86` → `out\Release\ijl15.dll`  
   - SHA256 与客户端 `ijl15.dll` 一致  
   - DLL 内含字符串 `SIX_RING_SAFE_20260726x`  
   - `config.ini`：`pendant2_ui=true`

## 3. 明确没有做错的事（吸取教训）

1. **未** patch pet HitTest（`0x801214` / `0x8013A3`）  
2. **未** 改错 VA / ModRM / 循环 imm  
3. **未** 用 expanded `(336,165)` 或 +33 宽版布局  
4. **未** 再做 HitTest→Draw 右列对齐（保持 GetSlotXY←HitTest）  
5. **未** 把 BP22/23 停到宠物坐标  
6. **未** ForceDrawLoop / DllMain 早挂 UI  
7. AbleToWear **先 orig 再 OK**（非半截 early-return）  
8. **未** 不安全扩大 `g_aEquipSlotPos` 表体  
9. 部署来自 **`out\Release`**，不是 `ezorsia\out`

## 4. 六戒验证清单

1. `pendant2_ui=true`；戳记 `SIX_RING_SAFE_20260726x`；SHA 与 `out\Release` 一致  
2. Login → 选角 → 进图：**无** ALL_IDLE / E_POINTER  
3. 武器在「武器」、盾在「盾牌」；护肩红 8；第二坠红 7  
4. **拖戒**到红 3/4 → 装备 −52/−53，图标在格上、**无红**  
5. **双击/自动穿**：空位依次填满 4 经典戒后，第 5/6 进 −52/−53  
6. **属性**：穿卸 −52/−53 后面板四维/攻魔随 `recalcEquipStats` 变  
7. 换图/重登后 −52/−53 仍在装备栏（`addInventoryInfo` 已放行）  
8. 回归：原 4 戒拖放/替换仍正常  

## 5. 相对 26w 的剩余风险（知情）

- 若进图仍 E_POINTER：立刻把 `kEnableExtraRingsBp52_53` 改回 `false`，仅保留 is_correct + 服务端路由 + 包同步，再排查具体调用点。  
- 选角 AvatarLook 仍不显示扩展戒外观（有意跳过）。
