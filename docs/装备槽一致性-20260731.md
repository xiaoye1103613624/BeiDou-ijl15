# 一致性审计与修复 CONSIS_6RING_BADGE_TOTEM_20260731

## 一致性矩阵（Client | Server | Match?）

| # | 项 | Client (ijl15) | Server (BeiDou) | Match? |
|---|----|----------------|-----------------|--------|
| 1 | EquipSlot / bodypart | 111→{52,53,12,13,15,16}; 118→54; 120→55; AbleToWear OK | `RING(-12/-13/-15/-16/-52/-53)` `BADGE(-54/-154)` `TOTEM(-55/-155)` | **是** |
| 2 | Login getCharInfo −51..−55 | apply-max **强制 55**; ApplyEquipZRef→影子; HT 独立表 | `addInventoryInfo` 放行 −52..−55/−154/−155（仅跳 −59） | **是** |
| 3 | migrateCash −152/−153/−154/−155 | Get/Set 别名 −154/−155→−54/−55；不 remap −152 进图 | `migrateCashRingsOffExtendedSlots`：**已接线**到 PlayerLoggedin（此前仅定义未调用）；**不**搬 −52/−53 | **是**（需重启服务端加载新类） |
| 4 | 拖拽 dst | 客户端发明确 dst；服务端戒指无 preferEmpty/rotate | 已去掉 preferEmpty/rotate；点装戒归一经典 cash | **是** |
| 5 | 卸装 −52..−55 | SendChangeSlotPosition 主路径 + wear occ 影子；Rehook 进图后 | `unequip(src,dst)` 通用负槽 | **是** |
| 6 | 属性 recalc | Combat_OccShadow @77F879 读影子 | `recalcEquipStats` 遍历全部 EQUIPPED | **是** |
| 7 | UI BP52–55 | DllMain HT/GetSlotXY/BE27 + pendant2 draw max55 | AvatarLook 仍跳过扩展槽（有意） | **是** |
| 8 | 部署戳记 | Client_1 ≡ out\Release；vanilla EXE 0x640 | 源码已改；全量 mvn 被无关 Quest API 错误挡住 | **客户端已部署 / 服务端待成功编译重启** |

## 本轮修复

1. **客户端源码回退**：工作区曾把 ring caves `DISABLED` 且戳记退回 `aj`，与服务端/文档脱节；从 transcript 重放 ak→29c 并清理重复定义后重建。
2. **补齐** Combat/Wear/Apply 影子 CodeCave、`g_ring52`、apply-max55、徽章/图腾 bodypart、独立 HitTest 表。
3. **`Shoulder_Rehook`** 在 `DamageSkin::EnsureHooks` 之后挂载（LazyCompatInit）。
4. **服务端**：`migrateCashRingsOffExtendedSlots(player)` 在 `getCharInfo` **之前**调用（此前函数存在但从未调用）。
5. **禁止** 未完成 cd64：`kNativeCd64ExtendedSlots=false`（vanilla heap 0x640）。

## 验证清单

1. 完全退出客户端；确认 `ijl15_VERSION.txt` = `CONSIS_6RING_BADGE_TOTEM_20260731`，`pendant2_ui=true`
2. **服务端**成功编译并重启后：穿非点装戒到红 3/4 → 重登仍在；DB `position in (-52,-53,-54,-55)`
3. 徽章 118 / 图腾 120 → 红 9/10 有图标；双击卸；面板属性随穿卸变化
4. 拖戒到已占用红 3/4 → 只换该槽；六满背包双击仍轮询
5. E 开栏 / 护肩 / 第二坠：无卡死；勿部署未完成 cd64 EXE
