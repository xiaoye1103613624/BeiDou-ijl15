# 装备槽修正简报 2026-07-27ap（3/4 重登空栏 + 进图安全）

戳记：`FIX_RING34_PERSIST_SAFE_20260727ap`  
SHA256：`85E1DE417438D2F486CB9471A0352BA4AF000D8B2E7A30EBB2EBB3DD67CC7501`（out\Release ≡ Client_1）  
内容：= **al** caves-before-bound / apply max55 + **an** `kRemapCashExtendedRings=false`（无 am −152 remap）  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`  
配置：`pendant2_ui=true`  
服务端：**无需重建**（DB / `addInventoryInfo` 已放行 −52/−53；护肩 −20 同链路）

## 1. 现象与根因

| 现象 | 根因 |
|------|------|
| A/B 回滚到 **aj** 后进图 OK（卸魂戒），但红 3/4「又没了」 | **aj 缺 al**：登录 SetItem 影子未正确写入；**DB 仍有 −52/−53** |
| 护肩 −20 仍在 | −20 原生槽，无影子依赖；与 −52/−53 **同落库链路**，客户端 apply 路径不同 |
| 为何不是「落库丢了」 | SQL 证据：`characterid=1` 仍有 `(-52,1113073), (-53,1113075), (-20,1152199)` |

## 2. 对比（护肩 vs 红 3/4）

| 项 | −20 护肩 | −52/−53 红 3/4 |
|----|----------|----------------|
| 落库 | ItemFactory / EQUIPPED | 同 |
| 登录包 | `addInventoryInfo` 放行 | 同（非 AvatarLook） |
| 客户端槽 | 原生 Char ZRef | −52 空隙；−53 **g_ring53ZRef 影子** |
| aj | 正常 | apply/bound/cave 顺序错 → UI 空 |
| al+an / **ap** | 正常 | caves 先装再升 bound；apply max55；无 cash remap |

## 3. 修复

- 戳记 `FIX_RING34_PERSIST_SAFE_20260727ap`
- 保留 `kRemapCashExtendedRings = false` — 魂戒勿落 −152/−153
- **禁止**为进图 A/B 单独回滚到 aj（会丢掉 al 持久化）

## 4. 验证清单

1. DLL 串 `FIX_RING34_PERSIST_SAFE_20260727ap`，SHA ≡ Client_1；无 `FIX_RING_CRASH_20260727am`
2. 重登 → 红 3/4 可见（DB 已有 −52/−53 时无需重穿）
3. 进图 OK（未装备魂戒于扩展槽；点装戒走经典 −112…）
4. 护肩 −20 无回归；E / 第二坠无卡死

## 5. 教训

**Rolling back Client_1 to aj for enter-map A/B loses al persist** → 红 3/4 看起来「又没了」，其实库里还在。
