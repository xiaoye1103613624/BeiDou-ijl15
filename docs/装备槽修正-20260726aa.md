# 装备槽修正简报 2026-07-26aa（红 3/4 库存绑定）

戳记：`RING_34_BIND_20260726aa`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
SHA256：`D1610D5AD6A6D734426EFC735278F6C3B74D2032D17B9FAE1382B2030EDE9915`（out ≡ 客户端）  
配置：`pendant2_ui=true`

## 1. 为何 z 仍无图标 / 无限拖 / 无 tip（根因，有证据）

| 核查 | 结果 |
|------|------|
| Client_1 DLL 是否 z | **是** — SHA ≡ out，串 `RING_ICON_ONESHOT_20260726z` |
| HitTest (104,35)/(137,35) | 运行时表写好后 → **BP52 / BP53**（PE 模拟 + skip 过滤已核） |
| 绘制 imm 0x32→0x34 | PE 位点正确：`7FEFB9 setne` + `7FEFBC add 0x32`；**不是**错函数 |
| **GetItem @42834E** | `cmp eax,-51` 后，`-100≤slot<-51` **直接清空返回**（目标 `4283EC`：`and [out+4],0; ret`） |
| **SetItem @47B0BF** | 同界：`-52/-53` 走失败返回 → **客户端永不写入** |
| 服务端 | `EquipSlot.RING` 已含 −52/−53；`addInventoryInfo` 已放行；双击兜底路由保留 |

**结论**：UI HitTest/坐标/绘制上界在 z 已基本到位，但 **客户端库存 GetItem/SetItem 把 −52/−53 当空洞**。拖放包可到服务端，客户端槽位永远空 → 无图标、无 tip、可无限再拖。

附加：即使放宽边界，−53 的原生寻址会落到 `Character+0x293`（TSecType[0]），会砸第一格；故 −53 必须进 DLL 侧 ZRef 影子槽。

## 2. 本次修复（aa）

### 客户端 `shoulders.cpp`

| 项 | 做法 |
|----|------|
| 边界 | GetItem / SetItem / 战斗 `cmp -51 (CD)` → **`cmp -53 (CB)`** |
| −52 | 走原生地址，落入 TSecType 前 **8 字节空隙**（安全） |
| −53 | GetItem/SetItem 地址计算 cave → **`g_ring53ZRef`**（避砸 TSecType） |
| 绘制 has_item | BP53 时改读影子 ZRef，并把 `[ebp-14]` 指过去 |
| 其余 | 保留 z：draw imm→0x34、HitTest→BE240C、三表坐标、get_bodypart×6、AbleToWear |

### 服务端

| 项 | 状态 |
|----|------|
| `EquipSlot.RING` −52/−53 | 保持（注释戳记改 aa） |
| `addInventoryInfo` 放行 | 保持 |
| 双击满 4 戒路由 | 保持 |

### 明确没做

- 未 ForceDrawLoop / 未改 pet HT  
- 未扩 `g_aEquipSlotPos` 表体  
- 未改武器/盾/护肩⑧/第二坠⑦ 逻辑  

## 3. 验证清单

1. SHA：`out\Release\ijl15.dll` ≡ Client_1；串 **`RING_34_BIND_20260726aa`**（无 z）  
2. `pendant2_ui=true`；Login→选角→进图：无 ALL_IDLE / E_POINTER  
3. 拖戒到红 3 → 槽位有图标、再拖应换装/拒绝（不再无限吞）  
4. 拖戒到红 4 → 同上  
5. 悬停红 3/4 → 有 tip/属性  
6. 经典 4 戒、武器、盾、护肩⑧、第二坠⑦ 正常  
7. 换图/重登后 −52/−53 仍在（服务端放行 + 客户端可读）  

## 4. 回退

- DLL：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260726_214257.bak`（或上一戳记）  
- 若仅 −53 异常：可临时关 `kBindExtraRingInventory`，保留 UI；再查 ZRef 赋值。  
