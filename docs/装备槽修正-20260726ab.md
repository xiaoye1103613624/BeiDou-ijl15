# 装备槽修正简报 2026-07-26ab（E 开装备栏卡死修复）

戳记：`FIX_EQUIP_UI_HANG_20260726ab`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
SHA256：`3336298440BB4C341DAE37D9FFEDC550593756BDF598AEF4E2CAFD0FF44C9501`（out ≡ 客户端）
配置：`pendant2_ui=true`

## 1. 挂死根因（aa，有代码证据）

| 核查 | 结果 |
|------|------|
| ForceDrawLoop @7FEFB9 | **未做**（setne 仍在 PE） |
| 错 VA / n-build ModRM | **否** — ExpectBytes 位点正确 |
| GetItem 钩递归 | **否** — 非 Detours 递归，是址算 cave |
| AbleToWear 早退 | **否** — 仍 call-orig |
| **DrawHasItem_Bp53_cave @7FEEDB** | **是** — 冒烟点 |

Client_1 PE：`7FEEDB: 8B 4D EC` = `mov ecx, [ebp-14h]`（位移 **0xEC = −20**）。

aa cave 写成了 MASM **`[ebp - 14]`（十进制 14 = 0x0E）**，与 PE 的 **`[ebp-14h]`** 不一致：

- `walk` 路径：几乎每次装备栏 has_item 都读错栈槽 → 野指针比较 / 后续绘制异常 → **按 E 开 CUIEquip 无响应**
- BP53 路径：把影子指针写到错误栈偏移，污染局部变量

次要风险（一并关掉）：GetItem/SetItem/战斗 `cmp -51→-53`、−53 `g_ring53ZRef` 影子。

**不是** ForceDrawLoop / DllMain 早挂 UI / HitTest 表越界那一类旧坑；是 aa 新引入的 has_item cave 栈偏移笔误。

## 2. 本次修复（ab）

| 项 | 做法 |
|----|------|
| `kBindExtraRingInventory` | **`false`** — 不装 bound 改写 / addr cave / draw has_item cave |
| DrawHasItem cave 源码 | 已改成 `[ebp-14h]`（供日后重开；当前不安装） |
| 保留（z 安全子集） | draw imm `0x32→0x34`、HitTest→BE240C、三表坐标、get_bodypart×6、is_correct/AbleToWear、护肩⑧/第二坠⑦ |
| ForceDrawLoop / pet HT | 仍禁止 |

## 3. 已知降级（可接受）

- 红 3/4（−52/−53）：客户端库存仍被原生 `cmp -51` 当空洞 → **可能无图标 / 可再拖**（回到 z 状态）
- 经典 4 戒、武器、盾、护肩、第二坠：应保持可用
- **优先保证：按 E 装备栏可开、不卡死**

## 4. 验证清单

1. SHA：`out\Release\ijl15.dll` ≡ Client_1；串 **`FIX_EQUIP_UI_HANG_20260726ab`**（无 aa）
2. `pendant2_ui=true`；进图后 **按 E → 装备栏打开、不卡死**
3. 经典戒图标仍在标签位
4. 护肩⑧ / 第二坠⑦ / 武器盾正常
5. （预期）红 3/4 绑定仍弱 — 待 ab 稳定后再用 `14h` 安全重开 bind

## 5. 回退

- aa 备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260726_214257.bak`（会再卡 E，勿随便回）
- 若 ab 仍卡：关 `kExtendCharDrawLoopToBp53` / `kEnableExtraRingsBp52_53` 回 y 级 UI，再报。
