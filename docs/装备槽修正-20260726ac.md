# 装备槽修正简报 2026-07-26ac（红 3/4 安全库存绑定）

戳记：`RING_34_BIND_SAFE_20260726ac`  
部署：`E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（禁止用 `ezorsia\out`）  
SHA256：`49D7ECB1080C1956437D0FF212C17C3798DCC10DEE964E56684D021D440BCC27`（out ≡ 客户端）  
配置：`pendant2_ui=true`  
备份：`BeiDou-Client_1\_ijl15_backup\ijl15.dll.20260726_220313.bak`（ab）

## 1. 相对 ab 的变更

| 项 | ab | ac |
|----|----|----|
| `kBindExtraRingInventory` | `false` | **`true`** |
| DrawHasItem cave | 源码已 `[ebp-14h]`，未安装 | **安装**（ExpectBytes `8B 4D EC…`） |
| GetItem/SetItem/战斗 bound | 未改 | `cmp -51(CD)` → **`cmp -53(CB)`** |
| −53 存储 | — | **`g_ring53ZRef`**（不进 TSecType） |
| −52 存储 | — | 原生 8 字节空隙（TSecType 前） |
| UI / HT / draw imm 0x34 / ×6 | 保留 | 保留 |
| ForceDrawLoop / pet HT | 禁止 | 禁止 |

前提：用户确认 ab 下 **E 开装备栏正常**。ac 仅重开 bind，cave 栈偏移与 PE 一致。

## 2. aa 教训（不得再犯）

- MASM `[ebp-14]`（十进制）≠ PE `[ebp-14h]`（`8B 4D EC`）→ E 卡死
- 勿部署 `ezorsia\out` 陈旧 DLL
- −53 不可直写进 TSecType；须影子 ZRef
- 勿 ForceDrawLoop / 勿 pet HitTest / 勿扩窄栏布局

## 3. 验证清单

1. SHA：`out\Release\ijl15.dll` ≡ Client_1；串 **`RING_34_BIND_SAFE_20260726ac`**（无 aa / 无 ab）
2. `pendant2_ui=true`；进图后 **按 E → 装备栏打开、不卡死**
3. 拖戒到红 3（−52）→ 有图标 / tip；再拖应换装或拒绝（非无限吞）
4. 拖戒到红 4（−53）→ 同上
5. 经典 4 戒、武器、盾、护肩⑧、第二坠⑦ 正常

## 4. 回退

- 若 E 再卡：立刻关 `kBindExtraRingInventory` 回 ab 戳记并 redeploy
- ab 备份：Client_1 `_ijl15_backup` 下最近 bak
