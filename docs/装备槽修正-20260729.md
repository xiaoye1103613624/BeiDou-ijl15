# FIX_APPLY_SHADOW_DBL_OUTER_20260729

## 根因

| 现象 | 根因 |
|------|------|
| 双击卸装仍失败（拖拽 OK） | `damageskinpicker` 进图后对 `@4EFD25` 二次 `DetourAttach`（独立函数指针），易把 DllMain 里 shoulders 的卸装钩子挤出调用链；仅靠 `ItemTI=1` 不够 |
| 全退/闪退后 −52..−55「丢了」（软退登录还在） | 登录 `getCharInfo` apply 常走 **直接** `lea ecx,[Char+esi*8+0xE7]` 写原生空隙，**绕过** `SetItem` 影子洞；软退不杀进程 → DLL 静态影子仍在 |

## 修复

| 改动 | 说明 |
|------|------|
| `ApplyEquipZRef_Shadow_cave` `@4E5D55` | BP52–55 → `g_ring52/53` / badge / totem 影子 |
| `Shoulder_RehookDblClickUnequipOutermost` | **进图** `DamageSkin::EnsureHooks` 之后再 `DetourAttach`（最外层）；DllMain 不再抢先挂 |
| damageskin `nPOS<0` | 跳过 GetItem 探测，直接 chain |
| 服务端 | 已有 `FIX_EXT_KEEP_5253`（不迁 −52/−53）；本戳无需再改 Java |

戳记：`FIX_APPLY_SHADOW_DBL_OUTER_20260729`
